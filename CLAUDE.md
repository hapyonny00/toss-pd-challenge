# CLAUDE.md — 작업 컨텍스트

Claude Code가 이 저장소에서 작업할 때 참고하는 문서입니다.

## 이 프로젝트가 뭔가

토스 2026 프로덕트 디자인 챌린지 제출물. 과제: **같은 회사 동료 6명이 다음 주까지 1시간 회의를 잡는 경험 설계.**

핵심 문제 정의(모든 결정의 기준점):
> 일정 조율의 진짜 고통은 빈 시간 찾기가 아니라, 모두가 안 될 때 **'누굴 포기할지'를 주최자가 혼자·미안하게 떠안는 것**이다. 시스템이 제약의 무게를 흡수해, 주최자가 판단도 죄책감도 지지 않게 한다.

## 편집 규칙

- **`prototype.html` 단 하나만 편집한다.** 빌드 없음. 브라우저로 바로 열림.
- `css/`, `js/` 폴더는 분리 버전(참고용). 실제 작업본은 `prototype.html`.
- 화면은 `<main class="page" id="p-xxx">`. id로 지시하면 정확히 찾아짐.
- 색·타이포·여백은 `:root` 토큰만 바꾼다. 개별 화면에 하드코딩 금지.

## 절대 지켜야 할 설계 원칙 (바꾸지 말 것)

1. **막다른 길 금지.** 모든 화면에 출구가 있어야 함(추천 다 별로 → 거래, 동료 다 안 됨 → 되는 시간 입력, 대기 → 5명 확정).
2. **묻지 말고 아는 걸 쓴다.** 캘린더 free/busy는 자동, 캘린더가 모르는 선호만 탭으로.
3. **One Thing per One Page.** 한 화면에 주 결정 하나. 주 버튼(파랑)은 화면당 하나.
4. **강조 리듬 3단.** 히어로(그림자) > 거래(파랑 틴트) > 차선(flat). 그림자 카드는 화면당 하나.
5. **라이팅은 사람 말로.** `docs/UX_WRITING.md`의 금지어(가중치·최적화·교집합) 쓰지 말 것.

## 화면 목록

| id | 화면 | 역할 |
|---|---|---|
| p-home | 회의 목록 홈 | 진입/재진입 |
| p-create | 회의 만들기 | 참석자 + 필수/선택 |
| p-add | 동료 추가 | 주소록 탭 선택 |
| p-collecting | 시간 모으는 중 | 캘린더 자동 + 응답 현황 |
| p-tap | 동료 10초 탭 | 안 되는 시간만 표시 |
| p-recommend | 추천 + 거래 | **히어로 화면** |
| p-ask | 조정 부탁 | 박지후께 대필 메시지 |
| p-waiting | 조정 대기 | 불안 없는 대기 |
| p-confirmed | 확정 | 자동 등록 |
| p-reschedule | 시간 바꾸기 | 대안 즉시 제시 |
| p-rescheduled | 변경 확정 | 조건부(박지후 대기) |

## 남은 할 일 (아이디어)

- [ ] 제출용 3대 질문 답안 (docs/SUBMISSION.md) — 배경·문제·가설·해결·결과·적용
- [ ] 단서 4개 → 설계 매핑 테이블 (제출물 서두)
- [ ] 추천 로직 다이어그램 (필수 가중 > 선호 회피 > 선택 참석)
- [ ] 피그마 이식 (docs/FIGMA_TOKENS.md 대로)
